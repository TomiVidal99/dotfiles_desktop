-- custom scripts that runs on certain filetypes

-- get files of a directory
local function get_files_in_directory(directory)
  local 
end

-- run an octave script on a new terminal tab
local function run_octave_script(script_name)
  local cwd = vim.fn.getcwd()
  local files = io.popen()
end
